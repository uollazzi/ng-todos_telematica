# Carrello

- creare un componente **carrello**
- creare una voce di menu per farlo apparire (come "auto" e "todo")
- il componente **carrello** è una versione estesa del carrello mini (ma che legge i dati dallo stesso **CarrelloService**)
- dal componente **carrello** deve essere possibile svuotare il carrello